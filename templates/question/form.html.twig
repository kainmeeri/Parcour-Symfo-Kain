{% extends 'base.html.twig' %}

{% block content %}
<h1 class="title_form">Ajouter une question</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Je soumet') }}</button>
{{ form_end(form) }}
        
{% endblock %}

           